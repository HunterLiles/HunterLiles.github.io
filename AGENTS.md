<!--
AUTOGENERATED FILE — DO NOT EDIT MANUALLY.

Source of truth: PROJECT.md
Generator: sync-agents

Run `sync-agents` after changing PROJECT.md.
The `dev` command also runs sync-agents automatically.
-->

# Agent Operating Instructions

PROJECT.md is the authoritative project specification. Read it before substantial work.
If this generated file and PROJECT.md ever disagree, PROJECT.md wins.

Current generated status: **Development**
Current generated language: **- Semantic HTML5.**

## Lifecycle Contract

### Planning

When the `# Status` section of PROJECT.md is `Planning`:

- Do not implement production code.
- Do not create source files, build-system files, manifests, or dependencies unless the user explicitly asks for a planning artifact.
- Do not begin "helpfully" scaffolding the implementation.
- Focus on refining the idea, scope, goals, non-goals, constraints, requirements, architecture, interfaces, risks, testing strategy, security requirements, performance expectations, MVP, and definition of done.
- Identify ambiguity and contradictions.
- Ask useful questions when answers materially affect the project.
- Present tradeoffs instead of silently choosing consequential architecture.
- Help edit PROJECT.md as decisions become clear.
- Do not rush the user into development.

A clear statement from the user such as "ready to start development", "let's start building",
"go ahead and bootstrap the project", or an equivalent explicit approval may transition the
project to Development.

When making that transition:
1. Perform one final consistency check of PROJECT.md.
2. Resolve or report genuine blockers.
3. Change the PROJECT.md Status to Development.
4. Run `sync-agents`.
5. Re-read AGENTS.md and PROJECT.md.
6. Bootstrap the project according to the approved specification.
7. Create/update project-memory Markdown under  rather than cluttering the repository root.
8. Do not commit or push unless explicitly instructed.

### Development

When the `# Status` section of PROJECT.md is `Development`:

- Treat the approved plan as the implementation target.
- Build rather than repeatedly reopening settled planning questions.
- Revisit planning only when implementation exposes a real contradiction, missing requirement, safety problem, or blocker.
- Prefer small, understandable, reviewable changes.
- Preserve stated scope and non-goals.
- Do not introduce dependencies or architecture that conflict with PROJECT.md.
- Keep PROJECT.md stable unless scope or requirements genuinely change.
- If PROJECT.md changes, run `sync-agents` immediately.
- Never commit or push unless explicitly instructed.

## Project Documentation Layout

Keep the repository root intentionally clean.

The only AI/project-control Markdown files that should normally live at the repository root are:

-  — authoritative human/project specification.
-  — autogenerated operational instructions derived from PROJECT.md.

Put other AI-facing project Markdown under , for example:

-
-
-
-
-
-
-
-

Rules:

- Create  if it does not exist.
- Do not move  or  out of the repository root.
- Do not scatter new AI-generated Markdown files across the repository root.
- Prefer updating an existing relevant file under  instead of creating redundant documents.
- Normal user-facing documentation such as , API documentation, manuals, or project docs may live wherever the project specification/build conventions require;  is specifically for agent/project-memory material.
- When bootstrapping Development, create , , and  when they are useful to the project.
- Treat  as version-controlled project context unless PROJECT.md explicitly says otherwise.

## Native-Code Verification Policy

This environment is designed primarily for C, C++, and Rust.

### Language not yet resolved

PROJECT.md does not currently identify C, C++, or Rust clearly.
While Planning, help resolve the language choice rather than inventing one.
During Development, do not bootstrap until the language/toolchain requirements are sufficiently clear.

## Reviewer Handoff

Before asking for review:

- run the project's deterministic checks;
- inspect `git diff` and `git status`;
- remove accidental/debug-only changes;
- make sure the implementation is consistent with PROJECT.md.

Copilot is used as a skeptical reviewer. Treat its findings as review input, not automatic truth.
Resolve confirmed issues in the implementation and rerun checks.

## Git Policy

- Keep changes uncommitted while an implementation/review cycle is in progress.
- Do not commit, amend, rebase, reset destructively, or push unless the user explicitly asks.
- Never hide failures by deleting tests, weakening checks, or changing requirements without approval.

## Security / Filesystem Policy

- Work inside the current repository unless the user explicitly grants another path.
- Do not read secrets, SSH private keys, password stores, credential files, or unrelated personal files.
- Never use `sudo` or install machine-wide packages as part of project implementation.
- If a machine dependency is missing, report the exact dependency and let the user decide how to install it.

## Current Project Parameters

The following is a generated snapshot of the authoritative PROJECT.md so that project parameters
are visible directly in this file. PROJECT.md remains the source of truth.

---

# Status

Development

# Project Name

Hunter Liles Portfolio Site

# Purpose

Create a fast, credible portfolio/CV website that helps Hunter Liles earn interviews for graphics-engineering roles while demonstrating the ability and curiosity to solve problems beyond graphics.

Present a technically serious early-career candidate whose mathematical, systems, and research foundations are supported by inspectable work. Visitors should understand his focus immediately and find convincing evidence within 30–60 seconds. The terminal aesthetic should reinforce his technical identity without interfering with normal web use.

# Intended Users

- Recruiters and hiring managers for graphics, rendering, systems, and software-engineering roles.
- Engineers and technical interviewers evaluating project depth.
- Professors, researchers, and graduate admissions reviewers as a secondary audience; industry hiring drives the primary presentation.
- Other visitors interested in Hunter's work or looking for a way to contact him.

# Core Use Case

A visitor opens the site on a computer or phone, immediately understands who Hunter is and what he specializes in, reviews his three strongest projects and relevant experience, and can reach his resume, GitHub, LinkedIn, or email without searching.

# Language

- Semantic HTML5.
- CSS, with Terminal.css as the primary design system.
- Vanilla JavaScript only where behavior cannot be achieved cleanly with HTML and CSS.
- No framework or frontend runtime unless a later requirement proves one necessary.

# Goals

- Present graphics engineering as Hunter's specialization and career direction, supported by his actual research and project experience; use only verified role titles.
- Lead with concrete technical evidence and make the site easy to evaluate quickly.
- Feature Signed Distance Field Research, Blossom, and the Algorithmic Trading System as the three flagship projects.
- Communicate breadth through the range of the work and this positioning statement: “Computer graphics is my niche, but I’m driven by learning new systems and solving difficult problems wherever they lead.”
- Use a strong Terminal.css aesthetic while preserving familiar website navigation and accessibility.
- Feel effectively instant on modern desktop and mobile devices.
- Keep the implementation small, legible, maintainable, and easy for future Hunter to change.
- Work intentionally on phones rather than merely shrinking the desktop layout.

# Non-Goals

- Do not build a terminal emulator, command-driven interface, or fake shell navigation.
- Do not use a JavaScript framework, component framework, CMS, database, or backend for the initial version.
- Do not add filler projects or sections merely to make the portfolio look larger.
- Do not create dedicated project case-study pages in the MVP.
- Do not turn the page into a dashboard of bordered cards.
- Do not add animation that delays, hides, or competes with information.
- Do not claim universal expertise; demonstrate curiosity and transferable engineering ability through evidence.
- Do not publish a phone number.

# Functional Requirements

## Page structure

The initial release is a single-page portfolio/CV with normal links and anchor navigation. Content should appear in approximately this order:

1. **Identity / introduction**
   - Hunter Liles.
   - Computer Science and Computational Mathematics.
   - Graphics, rendering, systems, and research focus.
   - The approved positioning statement.
   - A short statement of the opportunities sought, once Hunter confirms role type and availability; make openness to systems and broader software work clear without diluting the graphics focus.
   - Clearly visible links to GitHub, LinkedIn, resume, and email.
   - Borderless, concise, and understandable before the visitor scrolls.

2. **Selected work**
   - Signed Distance Field Research receives the most prominence.
   - Blossom and the Algorithmic Trading System sit alongside one another below SDF on sufficiently wide screens, with Blossom first in reading order.
   - Each project must explain:
     - the problem or motivation;
     - Hunter's personal contribution;
     - the technically difficult or interesting work;
     - relevant technologies, systems, or mathematics;
     - concrete outcomes, measurements, or evidence where available;
     - where the visitor can inspect the work further.
   - Project links and meaningful media should be obvious without overwhelming the text.
   - Start each project with a one-sentence summary and a few concise evidence bullets. Link to repository documentation for deeper analysis instead of placing a long case study on the homepage.
   - No placeholder snippets, placeholder media boxes, or generic filler copy may remain.

3. **Research and experience**
   - Use one reverse-chronological timeline unless the available content later justifies separate sections.
   - Include role, organization, dates, context, and concise accomplishment-oriented details.
   - Avoid repeating the SDF project's technical description; the timeline should explain the professional or academic context.
   - The timeline should feel open and should not require an enclosing card.

4. **Education and technical skills**
   - Show McNeese State University, degree programs, expected graduation, and only academic details that strengthen the application.
   - Present skills statically in meaningful groups such as languages, graphics, systems, and tools.
   - Skills should be supported by the work shown elsewhere and should not be animated or presented as proficiency meters.

5. **About and live renderer**
   - Place both below the primary qualifications and flagship work.
   - Pair them in the desktop grid when the content and available width support it; the renderer must not consume a full row without a content-driven reason.
   - Include an ASCII-to-photo headshot transition for hover and keyboard focus; on touch-only devices, show the real photo by default without requiring a hidden gesture.
   - Include a small live renderer demonstrating projection, transformations, and geometry—not a fourth flagship project or an engine-development project in its own right.
   - Briefly explain what the renderer demonstrates so it is not an unexplained decoration.

6. **Contact / footer**
   - Keep it minimal and borderless.
   - Provide email and the primary professional links.
   - Do not expose Hunter's phone number.

## Technical evidence and editorial standards

- Show engineering judgment, not just a technology inventory. Each project needs at least one concrete decision, its rationale or tradeoff, and how the resulting behavior was checked.
- Make ownership explicit: distinguish Hunter's work from collaborators' contributions, libraries, tutorials, and pre-existing tools. Credit sources appropriately, including any tutorial-derived renderer work.
- Label project status accurately: planned, in progress, or completed. Describe implemented behavior separately from future goals; an unfinished project can demonstrate useful working results without claiming completion.
- SDF Research should show the question investigated, Hunter's contribution, and actual rendering evidence. Explain supported techniques and limitations precisely; avoid sweeping claims such as perfect geometry or replacing rasterization generally.
- Blossom should demonstrate graphics-software implementation and design judgment through features that actually exist, such as rendering, shader work, mathematical routines, debugging, or architecture. Explain why a custom subsystem was useful, not merely that it was written from scratch.
- Algorithmic Trading should demonstrate transferable problem-solving through implemented systems work, such as data handling, testing, computation, or interface design. Mention GPU acceleration or other specific capabilities only with their verified implementation status. Keep performance claims about the software distinct from trading returns.
- Include at least one authentic screenshot or short, efficiently delivered recording for each graphics project, with a caption explaining what it proves. Prefer technical evidence over decorative media; do not generate images that pretend to be project output.
- When reporting benchmarks, include the workload or scene, hardware, relevant settings, units, and comparison baseline. Distinguish measurements from estimates. A correct working result or documented experiment is acceptable evidence when a useful benchmark is unavailable.
- Use confident, specific language without unsupported superlatives. Never invent metrics, publications, roles, credentials, dates, or experience. Keep unknowns in Open Questions and obtain verified content before release.
- Link directly to the relevant repository, branch, demo, or document. Before release, check that a visitor can find an overview, build/run instructions where applicable, and evidence of Hunter's contribution. Flag missing repository documentation for follow-up rather than expanding this website task into unrelated repository changes.
- Keep site and resume claims consistent. Include only skills Hunter can explain and relate to concrete work; do not pad the list with job-description keywords.
- Omit empty optional sections rather than inventing experience, honors, or publications to fill the layout.

## Interaction

- Navigation must behave like a normal website; terminal syntax may be used as visual language only.
- Core content and links must remain usable when JavaScript is unavailable.
- The renderer must pause when it is outside the viewport or the page is hidden.
- Provide a keyboard- and touch-accessible pause/resume control. Use a static fallback when animation is unavailable or not requested.
- Respect `prefers-reduced-motion`; provide a static renderer state and avoid nonessential transitions when reduced motion is requested.
- No Typed.js skill carousel, blinking or pulsing links, or interaction that hides information until an animation completes.
- ASCII morphing is optional and should be included only if it adds value without adding disproportionate code or dependency weight.

## Metadata and discoverability

- Include a useful page title and meta description.
- Include canonical and social-sharing metadata once the production URL and preview image are known.
- Use a logical heading hierarchy and semantic landmarks.
- Provide a favicon and appropriate preview image before final release.

# Performance Requirements

- Optimize for immediate comprehension and minimal work before first render.
- Target a Lighthouse mobile Performance score of at least 95, Largest Contentful Paint at or below 1.5 seconds, and Cumulative Layout Shift at or below 0.05. Use the median of three cold-cache runs against the production-served site with Lighthouse's default mobile throttling; record browser/tool versions and test settings so results are reproducible.
- Target interaction latency at or below 200 ms in manual click, tap, and keyboard tests, including while the renderer is active. A navigation-only Lighthouse run does not establish Interaction to Next Paint (INP); check interactions separately. These lab checks are not a claim about real-user field percentiles. See the [INP measurement guidance](https://web.dev/articles/inp).
- Treat these numbers as acceptance targets, not permission to add delay. Do not add loading screens, typing reveals, artificial waits, or delayed navigation. Network conditions prevent a universal instant-load guarantee.
- Keep initial JavaScript limited to the renderer and essential interaction code; no general-purpose dependency should be loaded for a single minor effect.
- Prefer CSS over JavaScript for presentation and interaction when the CSS solution remains readable and accessible.
- Do not load syntax highlighting unless meaningful code excerpts are present.
- Self-host and pin required production assets when doing so improves reliability and avoids avoidable third-party requests.
- Prefer a system monospace font; add a web font only when its visible benefit justifies the extra request and bytes.
- Use appropriately sized modern image formats, explicit image dimensions, and lazy loading for media below the first viewport.
- Avoid autoplaying heavyweight GIFs or video. Use efficient formats and defer playback/loading where practical.
- Pause all continuous rendering work when it is not visible.
- Use requestAnimationFrame for animation, initialize the renderer near the viewport, bound canvas resolution on high-density screens, and profile frame work on a phone. Keep page scrolling and input responsive; do not add workers or a rendering framework without a measured need.
- Production hosting must enable compression and sensible caching for static assets.
- Performance budgets may be tightened after real project media is selected, but should not be weakened merely to accommodate decorative content.

# Platform Requirements

- Support current stable Chrome, Firefox, Safari, and Edge.
- Provide intentional layouts for phones, tablets, laptops, and wide desktop screens.
- The smallest required viewport width is 320 CSS pixels.
- Desktop content must stay inside a centered, restrained maximum-width container rather than expanding to fill the browser.
- Multi-column regions must reflow into a deliberate single-column reading order on narrow screens.
- Keep DOM order consistent with visual reading order. Avoid page-level horizontal scrolling or clipped text at the required widths.
- Interactive targets must remain comfortably usable by touch.
- Hover may enhance an element but must never be required to discover content or perform an action.

# Architecture Preferences

- Use a small static-site architecture: one primary HTML document, one focused custom stylesheet, and one small JavaScript entry point unless a split clearly improves readability.
- Let Terminal.css provide the visual language and standard components. Do not recreate components it already solves adequately.
- Use CSS Grid as the underlying layout system, but keep the grid visually invisible.
- Allocate grid space according to content and importance; do not force every section to span the full content width or occupy an equal tile.
- Establish hierarchy in this order: spacing, typography, alignment, dividers, then borders.
- Add a border only when it communicates grouping, interaction, media framing, or importance.
- Keep custom CSS focused on the centered container, grid, responsive behavior, CRT treatment, media layout, headshot behavior, and small theme adjustments.
- Favor direct, obvious code over abstractions that save few lines or exist only for hypothetical future growth.
- Repetition may be abstracted when doing so clearly improves readability and maintenance; minimizing code must not make the code cryptic.
- Enhance progressively: the page's qualifications, project information, and links must exist in HTML, while JavaScript adds only nonessential visual behavior.

# Dependencies

- Terminal.css is the primary and expected visual dependency. Use a pinned version and verify its license before release.
- Avoid additional runtime dependencies by default.
- Do not use Typed.js. Add syntax highlighting only if meaningful source excerpts justify it.
- AsciiMorph is optional, not a default dependency; static ASCII artwork is sufficient unless morphing adds clear value.
- Do not introduce a package manager, bundler, or build framework unless it produces a clear, measured benefit that outweighs the added complexity.

# Data / Storage

- No database, account system, cookies, analytics, or application storage are required for the initial version.
- Site content is maintained directly in the repository.
- Project media, the resume, fonts, and other static assets should be stored in clearly named asset directories.
- Do not add third-party analytics by default. If analytics are proposed later, privacy impact, script cost, and actual decision value must be evaluated first.

# Interfaces

- A single public responsive web page.
- Standard anchor navigation and external links.
- Downloadable or directly viewable resume PDF.
- Email contact link.
- An embedded canvas used only for the live renderer.
- No CLI, API, server, authenticated area, or command parser.

# Security Requirements

- Treat the site as public and include no secrets, credentials, private keys, private documents, or nonpublic personal data.
- Do not expose Hunter's phone number.
- External links opened in a new tab must use safe `rel` attributes.
- Avoid third-party scripts; any retained external asset must be pinned, trusted, and justified.
- Use HTTPS in production.
- Add a Content Security Policy when the deployment platform supports it without unnecessary complexity.
- Collect no visitor data in the initial version.

# Error Handling Expectations

- Core content and navigation must remain available if JavaScript fails or is disabled.
- Renderer failure must be isolated: show a static fallback or short explanation without affecting the rest of the page.
- Missing media must not collapse the layout, hide project text, or create major layout shifts.
- External links should be checked before release; unavailable external resources must not prevent the page from loading.
- Production code should produce no uncaught exceptions or console errors during normal use.

# Testing Requirements

- Validate semantic HTML and check for broken internal and external links.
- Test the responsive layout at 320, 375, 768, 1024, and at least 1440 CSS pixels, plus representative intermediate widths.
- Smoke-test the supported browser engines, including Safari on iOS and Chrome on Android where devices are available. Responsive emulation checks layout but does not establish real-device performance; record coverage gaps honestly.
- Run the repeatable Lighthouse and interaction checks defined in Performance Requirements; report measured values and any unmet targets, not only a pass/fail score.
- Run an automated accessibility audit and manually verify keyboard navigation, visible focus, heading order, contrast, alt text, zoom to 200%, and reduced-motion behavior.
- Test with JavaScript disabled and confirm that all essential qualifications, project information, and contact links remain available.
- Verify that renderer animation pauses offscreen, while hidden, and under reduced-motion preferences.
- Check that images reserve space and that the page does not shift materially while loading.
- Add automated unit tests only for JavaScript logic complex enough to benefit from them; do not create a test framework for trivial DOM wiring.
- Do a 60-second content review with someone unfamiliar with the work: can they identify Hunter's specialty, describe one personal contribution and its evidence, recognize breadth beyond graphics, and find the resume/contact links? Revise content if the answers are unclear.

# Build Requirements

- A clean clone must be viewable with a simple local static-file server and must not require a compilation step for development.
- The initial implementation should not require a package manager or dependency installation.
- Use relative asset paths so local and production behavior remain consistent.
- Keep the repository README limited to the project title and a basic site description; maintain deployment settings in Cloudflare Pages.
- If a later optimization step is introduced, it must be deterministic, documented, and must not become necessary merely to edit content.

# Constraints

- Terminal.css must supply almost all visible styling that it can reasonably supply.
- The desired look is a terminal aesthetic, not a functional or simulated terminal.
- The site must prioritize professional and academic credibility over spectacle.
- Use subtle static CRT scanlines and vignette without flicker. They must not intercept input, obscure text or focus indicators, or add continuous rendering work.
- The strict layout grid must not become an obvious collection of equal boxes.
- Borders must be purposeful rather than automatic.
- SDF Research, Blossom, and the Algorithmic Trading System are the only flagship projects in the initial release.
- The live renderer stays below the primary work and shares space with other content when practical.
- Prefer the least code that solves the actual problem clearly. Simplicity must not sacrifice readability, accessibility, correctness, or measured performance.

# Definition of MVP

The MVP is a deployable, responsive single-page site that:

- clearly presents Hunter's identity and approved positioning statement;
- includes working GitHub, LinkedIn, resume, and email paths;
- presents complete, non-placeholder summaries for SDF Research, Blossom, and the Algorithmic Trading System;
- includes verified research/experience, education, skills, about, and contact content without empty filler sections;
- uses Terminal.css with a small custom responsive grid and restrained CRT treatment;
- includes the optimized live renderer and accessible headshot treatment below the primary work;
- works at 320 CSS pixels, with keyboard navigation, reduced-motion support, and useful non-JavaScript fallbacks;
- passes the agreed functional, accessibility, and performance checks;
- contains no filler projects, Typed.js carousel, blinking links, placeholder content, phone number, or fake terminal navigation.

# Definition of Done

- All approved content is accurate, proofread, and free of placeholders.
- The visual hierarchy communicates Hunter's specialization and strongest evidence within the first 30–60 seconds.
- The three flagship projects explain problem, contribution, technical depth, outcomes, and inspection links.
- Every flagship has inspectable evidence, an accurate status, a clear ownership boundary, and at least one explained engineering decision. Graphics media depicts actual project output.
- Desktop and phone layouts are intentional and visually verified.
- Terminal.css is the dominant design system, and custom code remains focused and readable.
- The site meets the agreed performance budgets and accessibility checks.
- Core information remains available without JavaScript, and renderer failure cannot break the page.
- Metadata, favicon, social preview, resume, and all production links are correct.
- No secrets or unnecessary personal information are published.
- Deployment from a clean clone is documented and repeatable.
- The production site is deployed over HTTPS and receives a final cross-browser review.

# Open Questions

## Approved development content (September 2026)

- Build the current draft with temporary copy or blanks for unanswered content questions; final release accuracy requirements still apply.
- Degree wording: Computer Science & Computational Mathematics (dual degree/major). The supplied resume gives expected graduation as May 2028.
- Website assets are stored directly under `public/assets/`; no duplicate source-asset directory is retained.
- Keep the current resume PDF unchanged, including its existing contact details, per Hunter's explicit instruction. This is an exception to the phone-number exclusion for the linked PDF only.
- SDF repository: https://github.com/daneboyd20001/SRPLightingEngine
- Blossom repository and branch: https://github.com/HunterLiles/Blossom/tree/Vulkan
- Trading repository: https://github.com/HunterLiles/Trading
- About copy may remain temporary. Unknown dates, project ownership details, and opportunity availability must not be invented.
- Hosting is Cloudflare with an existing custom domain; the domain and existing deployment configuration are not yet supplied.
- Store agent context under `.agents/`; configure Cloudflare Pages with `public` as its build output directory so project notes and testing tools are not deployed.

- What are the final degree names, expected graduation date, GPA/honors if included, and exact experience timeline entries?
- What final copy, measurable results, screenshots, diagrams, repositories, papers, presentations, or demos are available for each flagship project?
- What are the final resume file, email address, production domain, social-preview image, and favicon?
- Where will the static site be hosted, and what compression, caching, and security-header controls does that platform provide?
- What role types and availability should the introductory opportunity statement mention?
- Which contributions, working features, limitations, and evidence are confirmed for each project, and which features are still planned?
- What compact renderer scene best demonstrates the intended concepts without expanding its scope?
- Should any project eventually receive a dedicated case-study page after the single-page MVP is complete?

# Decisions

- Use the exact Miasma.nvim palette: charcoal background, parchment text, muted-gold links and labels, rust accents, and moss decorative lines.

- Use a single-page portfolio/CV for the initial release.
- Position Hunter as an aspiring graphics engineer whose mathematical, systems, and research foundations are demonstrated through actual work.
- Use the approved line: “Computer graphics is my niche, but I’m driven by learning new systems and solving difficult problems wherever they lead.”
- Target graphics-engineering roles first while demonstrating transferable problem-solving ability.
- Feature exactly three flagship projects: Signed Distance Field Research, Blossom, and the Algorithmic Trading System.
- Use Terminal.css as the dominant design system and embrace its aesthetic constraints.
- Use a terminal aesthetic without simulating terminal interaction.
- Use a centered, content-width-constrained, mixed grid whose structure is not visually obvious.
- Let content importance determine section width and pairing.
- Use borders sparingly and keep the experience timeline visually open.
- Keep CRT scanlines and vignette.
- Use the ASCII-to-photo headshot treatment with accessible phone behavior.
- Exclude Typed.js and blinking or pulsing links.
- Keep the live renderer below the flagship work, preferably paired with About, and do not present it as a flagship project.
- Favor a framework-free static implementation, minimal dependencies, and no required build system.
- Treat phone support, accessibility, performance, metadata, and graceful degradation as release requirements rather than later enhancements.
